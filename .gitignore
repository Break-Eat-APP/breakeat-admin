# Dependencies
node_modules
.pnp
.pnp.js

# Build outputs
dist
build
.next
out
coverage
*.tsbuildinfo

# Turbo
.turbo

# Environment files — NEVER commit real env files
.env
.env.local
.env.production
.env.*.local

# Secrets, service-account keys & signing material — NEVER commit
# (specific names only — do NOT blanket-ignore *.json, it would hide
#  package.json / tsconfig.json / vercel.json etc.)
firebase-app-distribution-key.json
firebase-adminsdk-*.json
**/google-services.json
**/GoogleService-Info.plist
service-account*.json
gcp-*.json
# Apple signing & App Store Connect API keys
*.p8
*.p12
*.mobileprovision
*.cer
*.certSigningRequest
# Android signing
*.keystore
*.jks

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode
.idea
*.swp
*.swo

# Claude Code — personal/machine-local settings (keep shared settings.json only)
.claude/settings.local.json

# React Native
apps/mobile/android/app/build
apps/mobile/android/.gradle
apps/mobile/ios/Pods
apps/mobile/ios/build
apps/mobile/.expo

# Docker
*.env.docker

# Sentry
.sentryclirc

# Local Claude tooling (preview launch config)
.claude/

# Configs Expo egarees a la racine.
#
# `eas` lance depuis un mauvais dossier remonte l'arborescence et, ne trouvant
# pas de projet, en fabrique un ICI : un eas.json sans variables d'environnement
# et un app.json portant un identifiant inconnu des stores. La build part alors
# sur un projet fantome et echoue a « Install dependencies ».
#
# La seule configuration valable vit dans apps/mobile/. Ces fichiers ne doivent
# jamais entrer dans le depot.
/eas.json
/app.json
