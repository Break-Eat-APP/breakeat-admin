import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // Phase 1 — shell only, no special config needed yet.
  // Phase 2+: add API rewrites to backend
  reactStrictMode: true,
};

export default nextConfig;
