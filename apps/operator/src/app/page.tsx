/**
 * Operator dashboard shell — Phase 1 placeholder.
 * Realtime dashboard implemented in Phase 8.
 */
export default function OperatorHomePage() {
  return (
    <main style={{ padding: '2rem' }}>
      <h1>BRAT EAT — Operator</h1>
      <p style={{ color: '#888' }}>Phase 1 shell — dashboard coming in Phase 8.</p>
    </main>
  );
}
